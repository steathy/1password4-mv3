# Signing key — SECRET. Losing/rotating it changes the extension ID.
build/key.pem
build/*.pem

# Build artifacts (regenerated by build/pack.ps1)
build/staging/
build/*.crx
build/install.reg
build/allowed_origins.snippet.txt

# OS / editor cruft
Thumbs.db
.DS_Store
